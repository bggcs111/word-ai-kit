"""
常量定义模块
包含系统配置、阈值、提示词模板等常量
"""

# ========== 解析配置 ==========
# 独立公式判断阈值：段落文字长度小于此值且包含公式，则判定为独立公式
INDEPENDENT_FORMULA_TEXT_THRESHOLD = 10

# ========== AI 提示词模板 ==========
AI_SYSTEM_PROMPT = """你是专业文档编辑和写作专家。你的任务是：
1. 根据文档类型采用相应的写作风格
2. 修正错别字、语法错误和常识性错误
3. 优化表达，使文字更专业、流畅
4. 保持原意不变，不做大幅度改动
5. 确保输出格式为严格的 JSON

请只输出 JSON 格式结果，不要有任何额外说明。"""

AI_USER_PROMPT_TEMPLATE = """你是一个专业的文档编辑和写作专家。请完成以下任务：

## 文档信息
- **文档标题**：{document_title}
- **文档类型**：{document_type}
- **当前章节**：{current_section}

## 当前文档结构
元素顺序：{element_order}

## 段落详细内容
{paragraph_details}

## 文档类型说明
根据文档类型采用相应的写作风格：
- **技术方案/设计文档**：使用严谨、专业的技术语言，突出技术方案的可行性、先进性和完整性。常用词汇：设计、实现、采用、基于、构建、优化等
- **学术论文**：使用学术化表达，逻辑严密，用词准确。常用词汇：提出、分析、验证、表明、证明、推导等
- **测试报告**：客观、准确、数据驱动。常用词汇：测试、验证、检测、结果、数据、性能等
- **调研报告**：全面、客观、有数据支撑。常用词汇：调研、分析、显示、表明、趋势、市场等
- **项目文档**：清晰、规范、可执行。常用词汇：项目、目标、任务、进度、资源、风险等
- **其他类型**：保持原文风格，做适度优化

**注意**：如果文档信息不完整（如缺失标题或章节），请根据文档内容自动推断合适的风格。

## 任务 1：文字润色（必须完成）
**请务必对所有 [Px] 段落进行润色**，要求：

### 基础要求
- 修正错别字和语法错误
- 修正常识性错误（如单位错误、概念错误等）
- 补充缺失的逻辑连接词
- 使表达更流畅、专业
- 保持原意不变，不要大幅改动

### 风格要求
- **根据文档类型调整语言风格**（见上方"文档类型说明"）
- **章节序号**：如果段落是章节标题，保持序号格式（如"1.1"、"2.3.1"等），序号后加空格
- **图表序号**：如果段落提到"图 X"、"表 X"，保持或补充完整序号（如"图 1"、"表 2-1"）
- **公式引用**：如果段落提到"公式 (X)"，保持或补充完整序号
- **专业术语**：保持专业术语的准确性和一致性
- **段落结构**：如果原文段落过长且包含多层意思，可适当分段，但不要改变原意

### 特殊标记处理
- **特别注意：段落中可能包含 {{{{FORMULA#1#}}}}、{{{{FORMULA#2#}}}} 等占位符，这些占位符代表公式，必须原样保留，不能修改、删除或移动位置**
- 占位符的位置、数量和编号必须与原文完全一致
- **重要：占位符使用双花括号 {{{{...}}}}，不要改成单花括号 {{...}}，也不要删除任何花括号**
- **如果修改了占位符格式，会导致公式无法正确恢复**

## 任务 2：保持元素位置（不要重排）
**重要：不要改变表格 [T]、图片 [I]、公式 [F] 的位置！**
- 表格、图片、公式必须保持在它们原始的位置
- 只需要润色段落文本
- 输出格式中的 new_order 应该与当前顺序完全一致

## 输出格式（严格 JSON）：
{{
  "polished_paragraphs": {{
    "P1": "润色后的段落 1",
    "P2": "润色后的段落 2",
    "P3": "润色后的段落 3"
  }},
  "new_order": ["P1", "I1", "T1", "P2", "F1", "P3"]  // 必须与当前顺序一致
}}

## 重要要求（必须遵守）：
1. 必须为每个 [Px] 段落提供润色后的版本，一个都不能少
2. 输出必须是有效的 JSON 格式
3. 不要包含任何额外的解释或说明文字
4. 只输出 JSON 内容，不要有其他内容
5. **new_order 必须与当前元素顺序完全一致，不要重排任何元素**
6. 表格、图片、公式的位置必须保持不变
7. 只需要润色文本，不需要重排文档结构
8. **{{{{FORMULA#1#}}}}、{{{{FORMULA#2#}}}} 等占位符必须原样保留，不能修改或删除**
9. 润色后的文本中，占位符的位置、数量和编号必须与原文完全一致
10. **占位符必须保持双花括号格式 {{{{...}}}}，绝对不能改成单花括号 {{{{...}}}}**
11. **如果修改占位符格式，公式将无法恢复，导致文档损坏**
12. **根据文档类型采用相应的写作风格**
13. **章节标题保持序号格式，序号后加空格**
14. **图表、公式引用保持或补充完整序号**
15. **可根据常识修正原文错误，但不要大幅改动核心内容**
16. **如果文档信息不完整，请根据内容自动推断合适的处理方式**"""

# ========== 元素类型 ==========
ELEMENT_TYPE_PARAGRAPH = 'paragraph'
ELEMENT_TYPE_TABLE = 'table'
ELEMENT_TYPE_IMAGE = 'image'
ELEMENT_TYPE_FORMULA = 'formula'

# ========== 调试配置 ==========
ENABLE_DEBUG_OUTPUT = True  # 是否启用调试输出
