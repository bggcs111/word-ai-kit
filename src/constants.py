"""
常量定义模块
包含系统配置、阈值、提示词模板等常量
"""

# ========== 解析配置 ==========
# 独立公式判断阈值：段落文字长度小于此值且包含公式，则判定为独立公式
INDEPENDENT_FORMULA_TEXT_THRESHOLD = 10

# ========== 文档类型风格要求模板 ==========
DOCUMENT_TYPE_STYLE_TEMPLATES = {
    "技术文档": """技术文档风格：严谨、专业，突出可行性、先进性。常用词：设计、实现、采用、基于、构建、优化。标题专业、使用技术术语。""",

    "学术论文": """学术论文风格：学术化表达，逻辑严密，用词准确。常用词：提出、分析、验证、表明、证明、推导。标题严谨，符合学术规范。""",

    "调研报告": """调研报告风格：全面、客观、有数据支撑。常用词：调研、分析、显示、表明、趋势、市场。标题客观，反映调研内容。""",

    "自媒体文章": """自媒体文章风格：轻松、口语化、有吸引力，注重可读性。常用词：不会还有人不知道、没想到、关键是、其实、干货。标题吸引人、有亮点。""",

    "保持原文风格": """保持原文风格：严格保持原文的写作风格、语气和用词习惯，只做必要的文字润色和修正。""",

    "其他": """其他类型：保持原文风格，做适度优化，标题简洁、明确、概括性强。"""
}

# ========== AI 提示词模板 ==========
AI_SYSTEM_PROMPT = """你是专业文档编辑专家。任务：
1. 【最高优先级】遵循用户自定义要求（如果有）
2. 根据文档类型采用相应写作风格
3. 修正错别字、语法和常识性错误
4. 补充内容使文档充实连贯（不胡编乱造）
5. 保持原意，输出严格JSON格式

只输出JSON，不要额外说明。"""

# ========== 统一提示词模板 ==========
AI_USER_PROMPT_TEMPLATE = """请完成文档润色任务。

## 【最高优先级】用户自定义要求
{user_prompt_section}
**以上用户要求必须严格遵守，优先于任何默认规则！**

---

## 文档信息
- **文档标题**：{document_title}
- **文档类型**：{document_type}
- **当前章节**：{current_section}

## 文档结构
元素顺序：{element_order}

## 段落内容
{paragraph_details}

## 文档类型风格参考
{document_type_style}
**注意：如果用户自定义要求与文档类型风格冲突，优先遵循用户自定义要求！**

---

## 核心任务

### 任务1：文字润色
**对所有 [Px] 段落进行润色：**
- 修正错别字、语法错误和常识性错误
- 补充逻辑连接词，确保段落衔接自然、逻辑连贯
- 删除冗余内容，统一术语表达
- 基于原文合理扩展，保持原意不变

### 任务2：标题处理
**文档标题：** 用户要求则生成，否则识别和保留原文标题

**章节标题：** 用户没有要求时不要生成章节标题
- 只有用户明确要求"添加章节标题"时才生成
- 生成时作为独立段落插入（不替换正文）
- 在 generated_chapter_titles 中返回，格式：段落ID:标题内容（如 P1:引言）
- 序号：用户要求"带序号"则生成（如"1 概述"），否则不添加

**图表标题：** 分析每个表格/图片前面的1个段落
- 统一处理：根据段落语义提炼标题放入 generated_chart_titles，保留原有序号（如"表1"、"图2"）
- 如果该段落是标题格式（如"图1 系统架构图"）：将其润色为普通正文描述（如"系统架构图展示了整体架构设计"），放入 polished_paragraphs
- 如果该段落是正文引用（如"系统架构见下图"）：正常润色，放入 polished_paragraphs
- 表格标题放表格上方，图片标题放图片下方

### 任务3：格式保持
- 占位符：{{FORMULA#X#}} 必须原样保留，不得修改、删除或改变位置
- 公式占位符在段落中的位置必须与原文完全一致
- 表格 [T]、图片 [I]、公式 [F] 位置不变
- new_order 与当前顺序完全一致

---

## 输出格式（严格 JSON）：
- document_title: 字符串，文档标题
- title_from_first_paragraph: 布尔值
- generated_chapter_titles: 对象，段落ID:章节标题
- generated_chart_titles: 对象，图表ID:图表标题（如 T1:表1 系统参数）
- chart_title_paragraphs: 数组，被识别为图表标题的段落ID
- polished_paragraphs: 对象，段落ID:润色后文本
- new_order: 数组，元素顺序

**注意：** chart_title_paragraphs 中的段落ID不要放入 polished_paragraphs"""

# ========== 元素类型 ==========
ELEMENT_TYPE_PARAGRAPH = 'paragraph'
ELEMENT_TYPE_TABLE = 'table'
ELEMENT_TYPE_IMAGE = 'image'
ELEMENT_TYPE_FORMULA = 'formula'

# ========== 调试配置 ==========
ENABLE_DEBUG_OUTPUT = True  # 是否启用调试输出
